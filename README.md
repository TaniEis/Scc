# Scc

## Installation

`npm i` / `yarn install`


## Starting the app

`npm start` / `yarn start`


## Build

`npm run build` / `yarn build`


## Tests

`npm test` / `yarn test`

`npm run test:coverage` / `yarn test:coverage`
